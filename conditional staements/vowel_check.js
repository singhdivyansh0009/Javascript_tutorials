/* program to check a letter is vowel or consonant */
let ch = prompt("enter a character");
    if (ch == 'a' || ch == 'e' || ch == 'i' || ch == 'o' || ch == 'u')
        console.log("character is vowel");
    else
        console.log("character is consonant");